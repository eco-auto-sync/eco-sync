plugins {
    id 'java-library'
}

dependencies {
    api project(':eco-sync-domain')

    implementation 'org.springframework:spring-context'

    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
}
